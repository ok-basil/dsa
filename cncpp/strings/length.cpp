#include <stdio.h>

int main() {
    char str[] = "Welcome";
    int i = 0;
    for (i = 0; str[i] != '\0'; i++)
    {
        
    }
    printf("The length of the string is %d\n", i);
    return 0;
}